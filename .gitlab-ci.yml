image: ubuntu:latest

scan:jsc:
  tags:
    - x86_64
    - linux
  only:
    refs:
      - schedules
  before_script:
    - mkdir /artifacts
  script:
    - bash -x ./scan-build-webkit.sh
  artifacts:
    paths:
      - /artifacts


